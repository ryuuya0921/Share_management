<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "思い出の図書館" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav>
      <ul>
        <% if user_signed_in? %>
          <li><%= link_to "ログイン設定", edit_user_registration_path %></li>
          <li><%= link_to "ログアウト", destroy_user_session_path, method: :delete, data: { confirm: "本当にログアウトしますか？", turbo: false } %></li>
          <li><%= link_to 'プロフィール', profile_path(current_user) %></li>
          <li><%= link_to 'ホーム', user_path(current_user) %></li>
        <% else %>
          <li><%= link_to "新規登録", new_user_registration_path %></li>
          <li><%= link_to "ログイン", new_user_session_path %></li>
          <li><%= link_to "ゲストログイン", users_guest_sign_in_path, method: :post %></li>
        <% end %>

        <% if user_signed_in? && @user.present? %>
          <% if @user.profile_image.attached? %>
            <%= image_tag @user.profile_image.variant(resize_to_limit: [50, 50]) %>
          <% else %>
            <%= image_tag 'default_profile_image.png', size: '50x50', alt: 'Default Profile Image' %>
          <% end %>
        <% end %>
      </ul>
    </nav>

    <%= yield %>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
  </body>
</html>

